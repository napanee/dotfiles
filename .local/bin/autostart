#!/bin/sh

# set cursor-size
xrdb ~/.Xresources

# keymapping
#[[ -f ~/.Xmodmap ]] && xmodmap ~/.Xmodmap

# notification daemon
dunst &

picom &

enpass &

nextcloud &

numlockx &

# --- Screen Lock & Power Management ---
# 10 min lock → +5 min monitor off → +15 min suspend
xss-lock --transfer-sleep-lock -- betterlockscreen -l &

xset +dpms
xset dpms 900 900 900

xidlehook \
	--not-when-fullscreen \
	--timer 600 'loginctl lock-session' '' \
	--timer 300 'xset dpms force off' '' \
	--timer 900 'systemctl suspend-then-hibernate' '' &
